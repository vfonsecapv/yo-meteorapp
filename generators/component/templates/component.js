import { Template } from 'meteor/templating';

import './<%= componentFileName %>.html';
import './<%= cssFileName %>';

Template.<%= componentName %>.helpers({
    
});

Template.<%= componentName %>.events({
    
});

Template.<%= componentName %>.onCreated(() => {
    const template = Template.instance();
    
    
});

Template.<%= componentName %>.onRendered(() => {
    const template = Template.instance();
    
    
});

Template.<%= componentName %>.onDestroyed(() => {
    const template = Template.instance();
    
    
});

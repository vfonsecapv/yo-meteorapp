import { Template } from 'meteor/templating';

import './<%= htmlFileName %>';
import './<%= cssFileName %>';

Template.<%= layoutName %>.helpers({

});

Template.<%= layoutName %>.events({

});

Template.<%= layoutName %>.onCreated(() => {
    const template = Template.instance();


});

Template.<%= layoutName %>.onRendered(() => {
    const template = Template.instance();


});

Template.<%= layoutName %>.onDestroyed(() => {
    const template = Template.instance();


});

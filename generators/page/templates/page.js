import { Template } from 'meteor/templating';

import './<%= htmlFileName %>';
import './<%= cssFileName %>';

Template.<%= pageName %>.helpers({

});

Template.<%= pageName %>.events({

});

Template.<%= pageName %>.onCreated(() => {
    const template = Template.instance();


});

Template.<%= pageName %>.onRendered(() => {
    const template = Template.instance();


});

Template.<%= pageName %>.onDestroyed(() => {
    const template = Template.instance();


});
